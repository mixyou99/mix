__pycache__/
.DS_Store
*.docx
*_RevisionReport.json
*_RevisionReport.csv
*_ExecutionLog.txt

# 원 논문 — 배포권 문제. 경로·해시만 기록한다
work/source/*.pdf

# work/ · cases/ 의 docx 는 케이스 산출물이다 — 위 *.docx 규칙(무관한 앱에서 온 것)에서 제외
!work/**/*.docx
!cases/**/*.docx

# npm 설치본 — 렌더러 의존(docx 9.7.1). 커밋하지 않는다
node_modules/
